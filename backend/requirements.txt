# Backend Gardens — Moteur de simulation d'ensoleillement

# Version actuelle: implémentation pure Python (stdlib) + Pillow.
# Les dépendances ci-dessous sont optionnelles et recommandées pour une
# version plus robuste ou plus performante à terme.

# --- Moteur de base (déjà fonctionnel) ---
Pillow>=9.0

# --- Pour l'API web ---
fastapi>=0.100
uvicorn[standard]>=0.23
python-multipart>=0.0.6

# --- Améliorations futures du moteur ---
# astral>=3.2          # calcul astronomique du soleil
# shapely>=2.0         # géométrie 2D
# numpy>=1.24          # grilles et calculs numériques
# matplotlib>=3.7      # rendu scientifique alternatif
